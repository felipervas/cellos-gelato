# Cellos Gelato (demo) — crachá

**O que é:** Gelateria fictícia para **demonstração na AGAGEL** — landing + painel de gestão completo (dados em `localStorage`).
**Status:** Demo pronta para a feira.
**Stack:** HTML/JS estático servido por `serve.cjs` (Node). Painel em `admin/`.
**Arquivos-chave:** `index.html`, `cartao.html`, `admin/`, `serve.cjs`, `img/`.
**Rodar:** `node serve.cjs` (ou abrir `index.html`).
**Entregas geradas →** `../_claude/entregas/cellos/`.
**Padrões:** seguir `../_claude/padroes/PADROES.md` + skill `padrao-sites`.
